# jaceyln wu · 工作台 安装到桌面

---

## 📱 手机桌面安装

本工作台已支持 **PWA**（渐进式 Web 应用），可以直接添加到手机主屏幕，效果和使用 App 几乎一样。

### 前提条件
- 工作台已部署到 GitHub Pages：`https://jaceyln-wu.github.io/jaceyln-wu-workbench/`
- 手机能正常访问这个网址

### iPhone / iPad

1. 用 **Safari** 打开 `https://jaceyln-wu.github.io/jaceyln-wu-workbench/`
2. 点击底部中间的 **分享按钮**（方框+向上箭头图标）
3. 向下滑动，找到 **「添加到主屏幕」**
4. 确认名称（默认显示「JW 工作台」），点击右上角 **「添加」**
5. 桌面出现图标，点击即可像原生 App 一样全屏打开 ✅

### Android

1. 用 **Chrome** 打开 `https://jaceyln-wu.github.io/jaceyln-wu-workbench/`
2. 浏览器右上角三个点 ⋮ → **「添加到主屏幕」**（或「安装应用」）
3. 确认弹出提示，点击 **「添加」** / **「安装」**
4. 桌面出现图标，全屏模式运行 ✅

> 如果未弹出安装提示，多等几秒让 Service Worker 缓存完成，然后重新打开页面即可。

---

## 💻 电脑桌面安装

### 方案一：Electron 原生应用（推荐）

将工作台打包成一个独立的桌面程序，有独立窗口和图标。

#### 步骤

```bash
# 1. 确保电脑已安装 Node.js（18+）
node -v

# 2. 进入工作台目录
cd /path/to/jaceyln-wu-workbench

# 3. 安装依赖（仅第一次）
npm install

# 4. 启动桌面版
npm start
```

#### 创建桌面快捷方式（macOS）

把项目文件夹拖进 Dock，或创建一个 `.command` 文件：

```bash
echo '#!/bin/bash
cd /path/to/jaceyln-wu-workbench
npm start' > ~/Desktop/jaceyln-wu启动.command
chmod +x ~/Desktop/jaceyln-wu启动.command
```

#### 创建桌面快捷方式（Windows）

在桌面新建快捷方式，目标填：

```
C:\path\to\node.exe C:\path\to\node_modules\electron\dist\electron.exe C:\path\to\jaceyln-wu-workbench
```

---

### 方案二：Chrome 快捷方式（Linux）

> `.desktop` 文件已生成，可直接使用。

#### 步骤

```bash
# 1. 复制桌面文件
cp jaceyln-wu.desktop ~/.local/share/applications/

# 2. 复制图标
cp icon-512.png ~/.local/share/icons/jaceyln-wu.png

# 3. 更新菜单缓存
update-desktop-database ~/.local/share/applications/
```

完成后在系统菜单搜索「jaceyln」即可找到，也可以固定到任务栏/Dock。

---

### 方案三：Chrome 快捷方式（macOS / Windows）

**macOS：**
1. Chrome 打开 `https://jaceyln-wu.github.io/jaceyln-wu-workbench/`
2. 右上角 ⋮ → **更多工具 → 创建快捷方式**
3. 勾选「在窗口中打开」→ 创建
4. 桌面出现独立窗口快捷方式

**Windows：**
1. Chrome 打开网址
2. ⋮ → **更多工具 → 创建快捷方式** → 勾选「在窗口中打开」
3. 桌面和开始菜单都会出现图标

---

## 📦 文件清单

| 文件 | 用途 |
|------|------|
| `index.html` | 主工作台（单文件，包含全部功能） |
| `manifest.json` | PWA 配置（手机安装必需） |
| `sw.js` | Service Worker（离线缓存） |
| `icon-192.png` | 手机桌面小图标 |
| `icon-512.png` | 手机桌面大图标 / 电脑图标 |
| `icon-maskable.png` | Android 自适应图标 |
| `apple-touch-icon.png` | iOS Safari 图标 |
| `package.json` + `main.js` | Electron 桌面版配置 |
| `jaceyln-wu.desktop` | Linux 桌面快捷方式 |
| `启动工作台.sh` | Linux 一键启动脚本 |
